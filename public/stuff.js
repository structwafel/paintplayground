// import { ChunkManager } from '../js/canvas.js';

// window.chunk = new ChunkManager(0, 0);
// console.log(chunk); // Check the entire chunk object
// console.log(chunk.ctx); // Check the ctx property specifically

import { ChunkManager } from '../js/manager.js';

window.chunk = new ChunkManager(0, 0);
